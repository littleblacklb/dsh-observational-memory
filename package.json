{
  "name": "dsh-observational-memory",
  "private": true,
  "type": "module",
  "description": "Observational memory for DeepSeek Harness: background observation, reflection, and pruning over the session log, with deterministic model-free compaction",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@vitest/coverage-v8": "^4.1.8",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
